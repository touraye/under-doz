# Banking System

You are task to simulate a banking software, you will build a robust system by leverage **abstraction**. Your project need to be in a package `bankingSystem`:

1. Create an **interface** called `Features`: 
   1. Create two methods: `deposit` and `withdrawal`, both will return a Boolean value and accept a double type of amount. 
2. ###### Create class called `Account`:

   1. Make the `Account` class implement the `Features` **interface** 
   2. Provide an implementation for the unimplemented methods 
3. Create a `Tester` class and create a `Account` object 



Task for Assignment 3:

After identifying all the common attributes and feature of the different bank accounts. 

1. Create a parent class called Account
   1. List all common attributes as fields/instance variables
   2. Create a constructor and it should take few arguments example (`String accountNumber, String accountHolderName, String branch`). Other 
   3. Define abstract method for the behaviors
2. Create child/sup class for the different bank accounts
   1. Define attribute for each individual child class.
   2. Define behaviors for each individual child class.



Task for Assignment 4:



































