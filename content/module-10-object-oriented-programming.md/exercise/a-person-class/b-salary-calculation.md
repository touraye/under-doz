# Exercise

Your Teacher Class defines some behavior, among them is `isQualified`, which performs a comparison operation on the `yearsOfExperience ` value against `4`. 

Your task is to determine whether a Teacher falls in the salary category of Grade 1 or 2. For the Grade 2 salary category, a teacher must be qualified, while for the Grade 1 category, they are unqualified. 

**Step 1** - Create a function called `getSalaryAmount`

This function should return the salary amount based on the Teacher's qualification.

**Step 2** - Testing your function:

Suppose a qualified Teacher receives `25000.00` and an unqualified teacher `18000.00`.