# Lab 1
Instructions:
1. Create a new Java project called `lab1`
2. Create a `main` method
3. Create a variable that will hold the name of a student
4. Create a variable that will hold GPA of a student
5. Print the name and GPA of a student using a print statement

The expected output:
```java
My name: Ahmed Dick
My GPA: 1.9
```

**Lab 2**

instructions: Cut down a piece of paper, write your name, your student number, and answer the following questions.

Time:  20m

1. Distinguish between a statement and a expression:

   ```java
   int x = y * 5;
   ```

2. Below are two ways of creating a variable. Write down what `a`and `b` is:

   ```java
   //a
   boolean flag;
   //b
   boolean isActive = false;
   ```

3. What expression are listed below: 

   ```java
   // a
   400 * (100 / 50);
   // b
   60 > 4 || 75 == 70;
   ```

4. Write the output of the following:

   ```java
   50 == 50 || !(40 != 40)
   ```

5. ```java
   boolean flag = true
   if (!flag) {
   	System.out.println("Yes")
   } else {
   	System.out.println("No")
   }
   
   // write what will be printed
   ```

   