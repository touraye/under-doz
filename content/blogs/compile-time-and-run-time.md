# Compile Time and Run Time

Java is a primary a **compile time language**, but it also involves a  **runtime time execution**. Some other things happens at compile type while other executions happens at runtime. Java couples these robust mechanisms making it such a power and a dynamic language. In this blog I will unfold to you what is compile-time- execution and runtime-time-execution. Things that happen at compile-time and runtime-time.

 