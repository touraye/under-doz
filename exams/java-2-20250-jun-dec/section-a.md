1. In programming, when a block of code/function calls itself repeatedly is said to be__
   1. Method overloading
   2. Method overridden
   3. Recursion
   4. Polymorphism
2. A Recursive call without a **base case** will lead __ error.
   1. IndexOutOfBound
   2. Stackoverflow
   3. InpustMisMatched
   4. None of the above
3. Using a Recursive Tree to solve the Fibonacci Sequence: `fibo(n)` is said to be the __ of the tree.
   1. Right child
   2. Left child
   3. Brand
   4. Root
4. When the left child of a Recursive Tree is `fibo(n - 1)'`, what is the formula for the right?
   1. `fibo(n - 1)`
   2. `fibo(n - 1)` + `fibo(n - 1)`
   3. `fibo(n - 0)`
   4. `fibo(n - 2)`
5. In computer science, time and space are the two fundamental resources required for executing any operation. 
   1. True
   2. False
6. What is the mathematical way to describe how fast an algorithm grows (its time complexity) and how much extra memory it uses (its space complexity) as the input size increases?
   1. Object Oriented Programming
   2. Big O Notation
   3. Data Structures
   4. Fibonacci Sequence
7. In Object-Oriented Programming, a class is created first and defines the properties and behaviors of an object before the object is created.
   1. True
   2. False
8. In Object Oriented Programming, object creation happens at ___
   1. Compile time
   2. Run time
   3. a and b
   4. None of the above
9. ___ also acts a blueprint for other classes.
   1. Final class
   2. Enum
   3. Abstract class
   4. Interface
10. __ is a type of class in Java that is used to define predefined types that other classes/objects can use.
    1. Enum
    2. Constructor
    3. Interface
    4. Abstract class
11. Java provides built-in Data types, which are primitive types, while programmers can create __ define types.
    1. Compiler 
    2. User
    3. Command line
    4. None of the above
12. A Data Structure where values are arranged in sequence is called__.
    1. Linear data structure
    2. None linear data structure
    3. All of the above
    4. None of the above
13. An Array's values are called items/elements, while a LinkedList's are___.
    1. Class
    2. Branch
    3. Node
    4. Object
14. Internally, all the Data structures are arrays in Java.
    1. True
    2. False
15. A Data structure where a value is mapped with a key-value pair.
    1. LinkedList
    2. Stack
    3. Queue
    4. HashMap