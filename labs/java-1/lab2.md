# Lab 2

Answer all question, 5 marks

1. The code below will user access or denied whether been an adult or child:

   ```java
   int age = 18;
   if () {
   	// code here
   } else {
   	// code here
   }
   ```

   complete the code; when the condition test for adult print appropriate print statement in the `if` block and the `else`. 

2. Write a three different cases for below `switc` statement:

   ```java
   String option = "a";
   switch (option) {
   
   }
   ```

   